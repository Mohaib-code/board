const authConfig = {
    providers: [
        {
            domain: "https://proud-albacore-77.clerk.accounts.dev",
            applicationID: "convex",
        },
    ],
};

export default authConfig;
